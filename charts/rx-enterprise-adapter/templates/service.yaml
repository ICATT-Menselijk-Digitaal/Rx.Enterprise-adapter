apiVersion: v1
kind: Service
metadata:
  name: {{ include "rx-enterprise-adapter.fullname" . }}
  labels:
    {{- include "rx-enterprise-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rx-enterprise-adapter.selectorLabels" . | nindent 4 }}
